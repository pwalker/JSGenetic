/*

a provider will provide endless (almost) chunks of text!  It is essentially an interator.

boolean provider.hasNext();
string provider.next();

*/
var grader = function (corpusProvider, candidateProvider) {
	


};
