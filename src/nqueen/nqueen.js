var nqueen = function (size, candidateProvider, options) {

/*

	// ensure object is of the right format!
	boolean validate(candidate);

	// mutate the right amount of the candidate, possibly paying attention to the options
	void mutate(candidate);

	// mixup the two candidates, possibly paying attention to options
	void crossover(candidate1, candidate2);

*/

};